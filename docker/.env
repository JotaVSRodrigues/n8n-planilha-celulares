# n8n local .env
N8N_HOST=localhost
N8N_PORT=5678
N8N_PROTOCOL=http

# Basic auth (recomendado, mesmo localmente)
N8N_BASIC_AUTH_ACTIVE=true
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=troque_uma_senha_forte_aqui

# Postgres DB
DB_TYPE=postgresdb
DB_POSTGRESDB_HOST=postgres
DB_POSTGRESDB_PORT=5432
DB_POSTGRESDB_DATABASE=n8n
DB_POSTGRESDB_USER=n8nusr
DB_POSTGRESDB_PASSWORD=troque_uma_senha_db_forte

# Encryption key — gere com openssl (veja abaixo) e cole aqui
N8N_ENCRYPTION_KEY=bo6aHMdf7gfCh6wUlebNTB9ZqSPyF9XT0K4hmeqPzv8=

# Webhook URL: se for local sem ngrok, use http://localhost:5678/
WEBHOOK_URL=http://localhost:5678/

